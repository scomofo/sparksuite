# Cleanup Handoff

## Status

This handoff has been reconciled.

The transitional instrument-generator `_final.js` files have already been folded into the canonical script names:

- `scripts/generate_instrument_pipeline.js`
- `scripts/apply_generated_instrument.js`

The canonical scripts now include the final behavior:

- `generate_instrument_pipeline.js` supports `--apply`
- `apply_generated_instrument.js` rebuilds the manifest, updates `index.html`, updates curriculum guardrail coverage, and runs validation unless `--skip-validate` is passed
- `generate_watch_preview.js` calls the canonical script names

## Active Generated Files

| File | Status |
| --- | --- |
| `js/instruments/instrument_manifest.generated.js` | Active, regenerated by `scripts/apply_generated_instrument.js` |
| `js/instruments/discovery_loader.js` | Available for manifest experiments; not production boot-wired because dynamic child scripts do not currently participate in the sequential deferred loader |
| `.superpowers/` | Ignored in `.gitignore` |

`index.html` intentionally keeps explicit `data-deferred-src` runtime instrument tags for deterministic boot order. The generated manifest remains the source of truth for tooling and diagnostics, but production loading should not switch to `discovery_loader.js` until that loader can signal completion to `SparkBootLoader`.

## Verification

Use these commands after instrument-pipeline changes:

```bash
node scripts/apply_generated_instrument.js
npm test
```

For a faster manifest-only check while iterating:

```bash
node scripts/apply_generated_instrument.js --skip-validate
```

## Remaining Notes

The old branch-hygiene guidance in this handoff referenced retired convergence branches and is no longer actionable. Current work should follow the repository's normal `master` checkpoint workflow and existing verification gates.
